library(tidyverse)
library(dplyr)
library(forecast)

Model_Testing <- function(Pool_Mod = Pool, Two_Way_Mod = Two_way, OLS = OLS_Mods){

    Init_Data = read_csv("data/Data_PWT.csv")

    Countries <- c("ARG", "AUS", "AUT", "BEL", "BRA", "CAN", "CHE", "COL",
                   "DEU", "DNK", "ESP", "FIN", "FRA", "GBR", "IND", "ITA", "JPN",
                   "MEX", "NGA", "NLD", "NOR", "PHL", "SWE", "THA", "TUR",
                   "USA", "VEN", "ZAF")

    Test_Data <- Init_Data %>%
        mutate(ln_Gdp = log(rgdpe)) %>%
        mutate(l_pop = log(pop)) %>%
        mutate(Labour_Ratio = emp/pop) %>%
        filter(countrycode %in% Countries) %>%
        mutate(l_cn = log(cn)) %>%
        select(countrycode, year, ln_Gdp, l_pop, hc, l_cn) %>%
        filter(year >= 2014)

    pdata_Test_Data <- pdata.frame(Test_Data, index = c("countrycode", "year"))

    #pred_Pool <- predict(Pool_OLS_reg, newdata = pdata_Test_Data)

    #ool_RMSE <- sqrt(mean((Test_Data$ln_Gdp - pred_Pool)^2))

    pred_Two_Way <- predict(Two_Way_Fixed_Mod, newdata = pdata_Test_Data)



    Two_Way_RMSE <- sqrt(mean((pred_Two_Way - Test_Data$ln_Gdp)^2))

    all_residuals <- c()
    all_predictions <- c()

    for (i in length(ols_results)) {
        model <- ols_results$model[[i]]

        country_data <- Test_Data %>% filter(countrycode == Countries[i])

        predictions <- predict(model, newdata = country_data)

        all_predictions <- c(all_predictions, predictions)

        residuals <- country_data$ln_Gdp - predictions

        all_residuals <- c(all_residuals, residuals)
    }

    OLS_RMSE <- sqrt(mean((all_residuals)^2))

    all_residuals_ARIMA <- c()
    all_predictions_ARIMA <- c()

    for (i in seq_along(arima_models$model)) {
        model <- arima_models$model[[i]]
        country_code <- arima_models$countrycode[i]
        country_data <- Test_Data %>% filter(countrycode == country_code)
        predictions <- forecast(model, h = nrow(country_data))$mean
        all_predictions_ARIMA <- c(all_predictions_ARIMA, predictions)
        residuals <- country_data$ln_Gdp - predictions
        all_residuals_ARIMA <- c(all_residuals_ARIMA, residuals)
    }

    ARIMA_RMSE <- sqrt(mean(all_residuals^2))

    Test_Data_mlrMBO <- Test_Data %>%
        mutate(countrycode = as.factor(countrycode))

    performance_mlrMBO <- predict(final_model, newdata = Test_Data_mlrMBO)
    mlrMBO_RMSE <- performance(performance_mlrMBO, measures = rmse)

    fitted_GBM <- performance_mlrMBO$data$response

    Test_Ensemble <- Test_Data %>%
        cbind(pred_Two_Way) %>%
        cbind(all_predictions) %>%
        cbind(all_predictions_ARIMA) %>%
        cbind(fitted_GBM) %>%
        rename(fitted_TW = pred_Two_Way,
               fitted_OLS = all_predictions,
               fitted_ARIMA = all_predictions_ARIMA) %>%
        select(ln_Gdp, fitted_TW, fitted_OLS, fitted_ARIMA, fitted_GBM)

    Ensemble_pred <- predict(final_model_Ens, newdata = Test_Ensemble)

    Ensemble_RMSE <- performance(Ensemble_pred, measures = rmse)

    print(performance_Ens)

    friendly_TW <- as.numeric(pred_Two_Way)

    plot_df <- Test_Data %>%
        cbind(friendly_TW) %>%
        cbind(all_predictions) %>%
        cbind(all_predictions_ARIMA) %>%
        cbind(fitted_GBM) %>%
        cbind(Ensemble_pred) %>%
        rename(fitted_TW = friendly_TW,
               fitted_OLS = all_predictions,
               fitted_ARIMA = all_predictions_ARIMA,
               fitted_Ens = response) %>%
        select(-truth, -l_cn, -hc, -l_pop)


}